FIRST: read /home/claude/work/agent_web_override.md — web access is ONLY via the Firecrawl connector tools (mcp__Firecrawl__firecrawl_scrape / mcp__Firecrawl__firecrawl_search; load them with ToolSearch if needed). Do not load the firecrawl-research skill, do not use the CLI, WebSearch or WebFetch. You are lane agent "NV". Budget: 20 searches, 120 scrapes. Stop when spent. Save sources to /home/claude/work/research/NV/, facts to /home/claude/work/research/NV/notes.md, failures to /home/claude/work/research/gaps.md prefixed [NV]. Check /home/claude/work/research/ for an existing file before every scrape. Do not retry errors.

SECOND: read /home/claude/work/agent_brief.md and follow it exactly EXCEPT its §6 (replaced by the override).

YOUR ASSIGNMENT — Needs Verification resolution (spec Phase 7/8). Work through /home/claude/work/needs_verification.jsonl (one JSON object per line: artist, album, year, note, _file). SKIP lines whose _file is staging/lane_C*.jsonl, lane_SW*.jsonl or lane_C6a.jsonl (Core sweep agents are handling those right now). For every other line: try to verify artist, exact title, original year, original label and 1–3 real key tracks (Wikipedia album/discography page, Discogs master page, Bandcamp, label site; query mode). If it verifies, write a full row (schema in the brief; lane = the lane of its _file unless it clearly fits another) to /home/claude/work/staging/lane_NV.jsonl. If it does not, skip it (it stays on the Needs Verification sheet) and add one line to /home/claude/work/research/NV/unresolved.md: artist | album | reason.
Known resolved facts you can use: Michael Chapman – Rainmaker (1969, Harvest; research/V8/wikipedia-michael-chapman-singer.md) only needs key tracks.
Before writing any row, build the set of staged (artist, album) pairs from every /home/claude/work/staging/lane_*.jsonl and never duplicate. Validate with python3 /home/claude/work/tools/validate_staging.py /home/claude/work/staging/lane_NV.jsonl until OK.
Reply with a SHORT summary: resolved count, unresolved count, scrapes/searches used, 429s.