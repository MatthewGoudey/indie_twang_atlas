FIRST: read /home/claude/work/agent_web_override.md — web access is ONLY via the Firecrawl connector tools (mcp__Firecrawl__firecrawl_scrape / mcp__Firecrawl__firecrawl_search; load them with ToolSearch if needed). Do not load the firecrawl-research skill, do not use the CLI, WebSearch or WebFetch. You are lane agent "SW1". Budget: 25 searches, 150 scrapes. Stop when spent. Save sources to /home/claude/work/research/SW1/, facts to /home/claude/work/research/SW1/notes.md, failures to /home/claude/work/research/gaps.md prefixed [SW1]. Check /home/claude/work/research/ for an existing file before every scrape. Do not retry errors.

SECOND: read /home/claude/work/agent_brief.md and follow it exactly EXCEPT its §6 (replaced by the override).

YOUR ASSIGNMENT — Core sweep of unfinished No Expectations leads.
Lists (format: artist | album | year | label | n_posts | URLs | note): /home/claude/work/sources/leftovers_C5.txt (54 albums, lane C5 twangy indie folk) and /home/claude/work/sources/leftovers_C8.txt (19, lane C8 modern slowcore/hushed twang). Also resolve these needs_verification lines in /home/claude/work/staging/lane_C8.jsonl: Lomelda Thx / M for Empathy / Hannah and Widowspeak Plum / Expect the Best / The Jacket (year+label already found; confirm key tracks from album pages, then write full rows into YOUR file — leave the nv lines in lane_C8.jsonl alone, the merge drops nv lines once a verified row exists).
Include each lead that fits a Core lane (C5/C8 or any other Core lane that fits better — set it and add borderline_note); reject with a reason those that do not fit (pop, ambient without roots texture, etc.). Anchors (Big Thief, Lenker, Meek, villagerrr) are done — skip.
Before writing ANY row, build the set of (artist, album) pairs already staged: read every /home/claude/work/staging/lane_*.jsonl (artist+album fields only, with a short python one-liner) and never add a duplicate. Lane definitions: /home/claude/work/skeleton/lanes.json. Output file: /home/claude/work/staging/lane_SW1.jsonl. Verification: Bandcamp /music pages (query mode: "list every release with year and label"), label catalog pages, Wikipedia. One artist page verifies a whole discography — include the artist's other fitting Core-era albums too. Every row's year, label and key tracks must come from a page you actually scraped (or a cached file under /home/claude/work/research/ — grep there first). Terry field = first No Expectations URL on the lead line. Append incrementally; run python3 /home/claude/work/tools/validate_staging.py /home/claude/work/staging/lane_SW1.jsonl until OK.
Reply with a SHORT summary: rows by primary lane, nv lines, items rejected (one line each), scrapes/searches used, gaps, 429s.
