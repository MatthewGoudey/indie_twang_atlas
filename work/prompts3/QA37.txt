FIRST: read /home/claude/work/agent_web_override.md — web access is ONLY via the Firecrawl connector tools (mcp__Firecrawl__firecrawl_scrape / mcp__Firecrawl__firecrawl_search; load them with ToolSearch if needed). Do not use the CLI, WebSearch or WebFetch. You are QA agent "QA37". Budget: 25 searches, 150 scrapes. Save sources to /home/claude/work/research/QA/, and do not retry errors.

YOUR ASSIGNMENT — KEY-TRACK and fact sweep of album rows written by OTHER agents. Earlier QA found that roughly 1 in 5 older rows lists 'key tracks' that are real songs by the artist but from a DIFFERENT album (or invented). Your main job: fetch each album's real track listing (Wikipedia album page '<Album>_(album)' or '<Album>_(<Artist>_album)', Discogs master, Bandcamp album page) and confirm every key track is on that album; if not, replace them with 1–3 real tracks from its listing (prefer singles/best-known songs the page names). Also confirm year, label and title while you are there. You did not write these rows; be skeptical.
Input: /home/claude/work/qa/batch_37.jsonl (one row per line: id, artist, album, year, type, label, key_tracks, sources, confidence, primary_lane). Rows are grouped by artist.
SEARCH IS SCARCE, SCRAPES ARE CHEAP: do not spend searches finding pages you can address directly. Try direct URLs first (each costs no search): https://en.wikipedia.org/wiki/<Artist>_discography, https://en.wikipedia.org/wiki/<Artist>, ..._(band), ..._(musician), https://en.wikipedia.org/wiki/<Album>_(album), https://<artist>.bandcamp.com/music. Use search only when two direct guesses 404. Never leave a row 'unchecked' just because an independent source is hard to find: if you cannot reach an independent page, re-check against the row's own cited source (scrape it in query mode) and record independent=false.
For each ARTIST: first grep /home/claude/work/research/ for an existing cached page about the artist (a discography or artist page) — you may use it, but PREFER a source that is different from the row's own `sources` so the check is independent (e.g. if the row cites Wikipedia, check Discogs master pages, Bandcamp, AllMusic or the label site; if it cites Bandcamp, check Wikipedia or Discogs). Use query mode, e.g. prompt "List every album, EP and live album with release year and original record label; for <album A>, <album B> also give the track listing." One page per artist usually covers all its rows.
For each ROW check: (1) the album exists under that exact title by that artist; (2) ORIGINAL release year; (3) ORIGINAL releasing label (reissue labels are wrong); (4) type (LP/EP/Live/Collab/Compilation); (5) key tracks really are on that album, when your page gives a tracklist.
Write one JSON line per row to /home/claude/work/qa/result_37.jsonl (append as you go):
{"id":"A0123","artist":"...","album":"...","verdict":"ok"|"fix"|"fail"|"unchecked","corrections":{"year":1994,"label":"...","album":"exact title","type":"EP","key_tracks":["..."]},"new_source":"https://... (the independent page you used, or \"\")","independent":true|false,"note":"short"}
- "ok": everything you could check matches. corrections = {}.
- "fix": the album exists but one or more fields are wrong — give ONLY the corrected fields.
- "fail": the album does not exist / is by someone else / cannot be a real release.
- "unchecked": you could not reach any independent page (say why).
Rules: never guess a correction; a correction must come from a page you scraped. Small title differences in capitalization/punctuation are "ok" unless the official title clearly differs. If the only disagreement is between sources (e.g. year 1996 vs 1997 for a late-December release), mark "ok" and explain in note.
Reply with a SHORT summary: rows ok/fix/fail/unchecked, error rate = (fix+fail)/checked, the list of fail ids, scrapes/searches used, any 429s.
