FIRST: read /home/claude/work/agent_web_override.md — web access is ONLY via the Firecrawl connector tools (mcp__Firecrawl__firecrawl_scrape / mcp__Firecrawl__firecrawl_search; load them with ToolSearch if needed). Do not load the firecrawl-research skill, do not use the CLI, WebSearch or WebFetch. You are lane agent "NET". Budget: 25 searches, 150 scrapes. Stop when spent. Save sources to /home/claude/work/research/NET/, facts to /home/claude/work/research/NET/notes.md, failures to /home/claude/work/research/gaps.md prefixed [NET]. Check /home/claude/work/research/ for an existing file before every scrape. Do not retry errors.

SECOND: read /home/claude/work/agent_brief.md and follow it exactly EXCEPT its §6 (replaced by the override).

YOUR ASSIGNMENT — network pass for the Core (Phase 2 §5 source 5–6).
1. LABEL CATALOGS (Core-era 2014–2026 releases that fit a Core lane): Dear Life Records, Sophomore Lounge, Curation Records, Keeled Scales, Lame-O Records, Exploding in Sound, Fire Talk, Paradise of Bachelors, Ruination Record Co., Orindal Records, Run for Cover (only its twangier end), Wax Nine, Darling Recordings, Loose Music. For each: scrape the label's Bandcamp or site catalog in query mode ("list every release with artist, title, year"), then pick releases that fit C1–C9 and are not staged yet; verify each (the label page itself counts as one source for year+label).
2. PEDAL-STEEL AND SESSION NETWORK: Xandy Chelmis (Lenderman/Wednesday steel player — solo records?), Red PK (C3 field-guide artist — check if staged), Spencer Cullum (C6 — check), Alex Farrar / Drop of Sun studio (Asheville) productions, Colin Miller's productions, Brad Cook productions that fit.
3. TERRY LEFTOVERS: /home/claude/work/sources/terry_by_lane/?.txt (27 undecided albums) — decide each (include in a fitting lane or reject with one-line reason in your reply). Skim /home/claude/work/sources/terry_by_lane/OUT.txt (254) for anything wrongly excluded that clearly fits a Core lane (e.g. twang, slowcore, slacker) — add at most 15.
Priority: C1 (currently thin, ~45 incl. anchors vs target 80) and C2 (28 vs 45) — favour releases that fit those lanes.
Target: 60+ new rows.
Before writing ANY row, build the set of (artist, album) pairs already staged: read every /home/claude/work/staging/lane_*.jsonl (artist+album fields only, e.g. with a short python one-liner) and never add a duplicate. Lane definitions: /home/claude/work/skeleton/lanes.json. Output file: /home/claude/work/staging/lane_NET.jsonl — rows may have any primary lane (Core, V or Context) as long as it fits; set borderline_note when the call is close. Verification: Bandcamp /music pages, label catalog pages, Wikipedia — query mode. Every row's year and label must come from a page you actually scraped (or a cached research file). Append incrementally; run python3 /home/claude/work/tools/validate_staging.py on your file until OK.
Reply with a SHORT summary: rows by primary lane, nv lines, labels/artists covered, judgment calls, scrapes/searches used, gaps, 429s.